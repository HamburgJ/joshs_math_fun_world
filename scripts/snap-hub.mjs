import { chromium } from 'playwright'; (async () => { const browser = await chromium.launch(); const page = await browser.newPage({viewport: {width: 800, height: 600}}); await page.goto('http://localhost:8080'); await page.waitForTimeout(2000); await page.click('body'); await page.waitForTimeout(500); await page.evaluate(() => { window.__JMFW__.josh.setPosition(0, 0, 0); window.__JMFW__.cameraCtrl.update(0.5, {mouseDeltaX:0,mouseDeltaY:0,scrollDelta:0}, window.__JMFW__.josh.getPosition()); }); await page.waitForTimeout(1000); await page.screenshot({path: 'snap-hub.png'}); await browser.close(); })();
